检测音频中以下声音事件的时间位置：{{EVENT}}
只输出合法 JSON 数组。每项必须且只能包含 type、start_time、end_time 三个字段。type 必须原样复制目标事件名称；start_time、end_time 必须是从媒体开头计起的十进制秒数（如 4.5、12.0），不是字符串，也不是分:秒或时:分:秒。相同事件出现多次就在同一数组中逐项列出。未找到时返回 []。不输出数组外的任何文字，不猜测未听清的声音。
