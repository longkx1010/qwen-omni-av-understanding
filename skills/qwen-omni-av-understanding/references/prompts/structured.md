请根据下面完整 JSON Schema，按场景和事件详细描述音视频内容。描述性字符串使用中文，保留 Schema 中的英文字段名。
所有时间戳相对于媒体开头；结束时间不得早于开始时间或超过媒体总时长；事件必须落在所属场景的范围内。场景和事件按时间顺序排列。只包含由音视频直接支持的信息，不猜测或编造；不要仅因声音与动作同时发生就推断因果。纯音频不要虚构画面。
只返回合法 JSON，不要 Markdown 围栏或解释。

{{SCHEMA}}
